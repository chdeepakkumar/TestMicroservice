spring:
  application:
    name: ApiGateway
  cloud:
    consul:
      config:
        enabled: false
      discovery:
        instanceId: ${spring.application.name}
      host: localhost
      port: 8500